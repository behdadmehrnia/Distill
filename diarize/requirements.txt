# Standalone pyannote diarization service (CPU).
# Install torch from the official CPU index first (see Dockerfile).

fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9
numpy>=1.26,<3
soundfile
pyannote.audio
