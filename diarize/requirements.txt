# Standalone diarization service (pyannote; optional NeMo via requirements.nemo.txt).
# Install torch from the official CPU/CUDA index first (see Dockerfile / runtime).

fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9
numpy>=1.26,<3
soundfile
httpx>=0.27
pyannote.audio==3.3.2
