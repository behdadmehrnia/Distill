# Distill — CPU-only runtime (no CUDA / torch / local ML)
# System dep: ffmpeg/ffprobe on PATH for pydub (mp3/m4a/ogg); Docker uses static binaries

fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9
httpx>=0.27

# Outbound STT/LLM HTTP client
aiohttp>=3.9,<4

numpy>=1.26,<3
python-dotenv>=1.0
pydub>=0.25
certifi>=2024.0.0
audioop-lts; python_version >= "3.13"

# Dev / tests
pytest>=8.0
pytest-asyncio>=0.23

# Auth
PyJWT>=2.8,<3
bcrypt>=4.0,<5

# Database
psycopg[binary]>=3.1,<4

# Higher-quality diarization (CPU torch + HF token) — also installed in Docker:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.optional.txt
